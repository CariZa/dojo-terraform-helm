# Default values for hellocloud.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: nginx
  version: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  public: true
